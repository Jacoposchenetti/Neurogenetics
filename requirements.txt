# Python 3.12 required (neuroimaging stack lacks 3.14 wheels as of 2026-07).
# Version ceilings pin to the last releases known-good on 3.12 to keep the
# environment reproducible. Loosen deliberately, re-log versions in results/logs.

# --- core scientific stack ---
numpy>=1.26,<2.2
pandas>=2.1,<2.3
scipy>=1.11,<1.15
statsmodels>=0.14,<0.15
matplotlib>=3.8,<3.10

# --- neuroimaging / imaging-transcriptomics ---
nibabel>=5.2,<5.4
nilearn>=0.10.4,<0.11
# abagen: PyPI 0.1.3 is incompatible with pandas 2.x (uses removed set_axis(inplace=)).
# Use the patched GitHub main (post-0.1.3) which supports pandas 2 / numpy 2.
abagen @ git+https://github.com/netneurolab/abagen.git   # AHBA expression (Fase 3)

# --- spatial-null / spin-test methods (Fase 4) ---
# netneurotools: used for dataset fetchers (mmpall, HCP standards, Schaefer) only —
# the spin generator was dropped upstream, so it lives in scripts/enrichment_lib.py.
# Upper bound guards the fetcher API; verified working on 0.2.3-0.3.0.
netneurotools>=0.2.3,<0.4    # dataset fetchers, parcellation utilities
brainsmash>=0.11.0           # spatial-autocorrelation-preserving surrogates

# --- gene-set / enrichment annotation (Fase 1 cross-check, Fase 6 output) ---
gprofiler-official>=1.0.0    # g:Profiler functional annotation

# --- figures ---
plotly>=6.0                 # vivid surface rendering (nilearn plotly engine, Fig 4)
kaleido>=1.0                # static PNG export of plotly figures

# --- convenience ---
tqdm>=4.66
pyyaml>=6.0
setuptools<81               # provides pkg_resources, which abagen 0.1.3 still imports
# jupyter intentionally NOT pinned here: its jupyterlab extension files exceed the
# Windows MAX_PATH (260) limit and abort the install. Install separately after
# enabling Long Path support, or just use VS Code's notebook support.
# ipykernel is enough to run notebooks in an editor:
ipykernel>=6.29
